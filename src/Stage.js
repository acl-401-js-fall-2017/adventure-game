import React, { Component } from 'react';

export default class Stage extends Component {
  constructor () {
    super();
    this.state = {
    
    };
  }

  render() {
      return (

      );
  }
}